version: '1'

services:
  client:
    image: portfolio:development
    build: 
      context: .
      target: dependencys
    volumes:
    - ./client:/app
    environment:
      - NODE_ENV=development
      - PORT=3000
    command: sh -c "npm run start --port 3000"
    ports:
      - 3000:3000
